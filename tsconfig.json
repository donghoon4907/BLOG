{
  "compilerOptions": {
    "target": "esnext", // 사용할 ECMAScript 버전 설정
    "module": "esnext", // 모듈 설정
    "jsx": "preserve", // preserve, react, react-native
    "lib": ["dom", "es2017"], // 컴파일에 포함될 라이브러리 파일 목록
    "moduleResolution": "node", //  모듈 해석 방법 설정
    "allowJs": false, // js 파일 컴파일 허용 여부
    "alwaysStrict": true, // 항상 strict mode로 분석할지 여부
    "esModuleInterop": true, // import namespace 생성을 통해 CommonJS와 ES Modules 간의 상호 운용성이 생기게할지 여부
    "noEmit": true, // 결과파일 저장 여부
    "allowSyntheticDefaultImports": true, // export default 를 export 한 값들을 가지는 객체로 설정 여부
    "forceConsistentCasingInFileNames": true, // 파일명에 대소문자 구분하지 않아도 되는 기능 사용 여부
    "noFallthroughCasesInSwitch": true, // switch문의 에러 발생 여부
    "isolatedModules": true, // 각 파일을 분리된 모듈로 트랜스파일 할지 여부
    "skipLibCheck": true, // 모든 선언파일(*.d.ts)의 유형검사를 건너뛸지 여부
    "noUnusedLocals": true, // 사용안된 지역변수에 대한 오류 보고 여부
    "noUnusedParameters": true, // 사용안된 파라미터에 대한 오류 보고 여부
    "resolveJsonModule": true,
    "removeComments": true, // 주석 삭제 여부
    "preserveConstEnums": true, // const enum형 선언을 지우지 않을건지 여부
    "sourceMap": true, // 소스맵(*.map) 파일 생성 여부
    "strict": true, // 모든 엄격한 타입 검사 옵션을 활성화 여부
    "strictNullChecks": true, // null과 undefined 타입 구분 여부
    "noImplicitAny": false, // 'any' 타입으로 구현된 표현식 혹은 정의 에러처리 여부
    "noImplicitReturns": true // 함수에서 코드의 모든 경로가 값을 반환하지 않을 시 에러보고 여부
  },
  "include": ["next-env.d.ts", "**/*.ts", "**/*.tsx"],
  "exclude": ["node_modules"]
}
